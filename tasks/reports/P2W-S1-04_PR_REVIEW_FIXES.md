# PR 4：首轮外部 Codex 审查修复

审查版本：269083058e69881da0f24ccda2498e1f49e683b3。

1. 部分未评分指标误判 FAIL：只将 correct < scored 视为实际错误，
   其余存在未评分项时为 REVIEW_REQUIRED，完全未评分仍为 NOT_SCORED。
   指标值继续使用完整 eligible 分母，不因不支持的公式而抬高覆盖率。
2. 默认 Office OMML 属性误报：接受空的 fPr/sSupPr/sSubPr/sSubSupPr、
   它们的 ctrlPr 纯样式内容，以及显式的普通分数 type=bar。
   未知属性和 noBar/skw 等非默认属性继续保留，不能等同于普通分数。

新增 9 个实际 DOCX 回归：混合支持/不支持公式并分别含正确或错误的支持项；
分数默认值、无分数线、斜线分数与三种上下标默认属性。
修复前 6 failed / 3 passed；修复后目标 41 passed。
独立检出的最终全量：544 passed，1 个既有 Starlette 弃用警告；
Ruff、mypy（58 个文件）、任务目录与模型基线校验全部通过。

只修改评估器与对应测试，不重算或改写封存样本、旧 DOCX 和既有报告。
外部审核需要对新提交重新完成，本记录不代表该新审核已经通过。

## 第二轮：额外图片与重复图片

审查版本 3d57b2b。此前按哈希存在性检查图片，会漏掉同一图片的额外出现，
也可能将一个实际绘图同时认领为多个期望图片。
现在以实际段落和绘图索引标识每次出现，每个来源图片消费一个匹配出现；
剩余出现报告 UNALIGNED_IMAGE，缺失出现报告 MISSING_OR_REPLACED_IMAGE。

新增 5 个实际 DOCX 回归：新段落重复图、同段落重复图、不同的新图片、
声明两次但只出现一次、声明两次且实际出现两次。修复前 4 failed / 1 passed，修复后通过。
图像夹具明确登记原始合成图片哈希，不从转换输出推导预期内容。
最终独立检出 549 passed；Ruff、mypy（59 文件）、两个既有校验器通过。
旧封存运行不回写，新提交仍须重新接受外部 Codex 审核。

## 第三轮：必要内容、非正文内容与完整零边标注

审查版本 bbfc5a6，三项反馈已分别补回归。

- 不支持的公式仍必须有实际非空 OMML 或完整源图；缺失或空容器报告 MISSING_FORMULA。
  未支持但确实保留的表达式保留未评分分母；其他源图的保留也不能覆盖既有内容失败。
- 检查正文所引用的页眉、页脚、脚注和尾注：有文字、公式、图片或域时明确报告
  UNSUPPORTED_VISIBLE_STORY。空页眉页脚允许，不能将未检查的可见内容默认为通过。
- coverage_complete=true 且无 uncertain 关系标注时，零确认边的所有预测边仍计入精度分母。
  不完整或含不确定关系的零边参考保持 NOT_SCORED，不能把未知当作确认无关系。

新增 15 个真实 DOCX 回归；同时将首轮“部分未评分”夹具完善为实际保留不支持的矩阵，
避免用缺失表达式冒充仅语法未支持。目标 61 passed，最终独立检出 564 passed；
Ruff、mypy（60 文件）与两个既有校验器全部通过。
旧封存运行保持不变，新提交继续等待最新版本的外部审核结论。

## 第四轮：冻结清单保护、源图降级与未支持正文

审查版本 43f84ee，三项反馈分别修复。

- seal 必须包含 manifest.json 与所选 corpus、annotation 成员，缺项在创建输出和启动转换前拒绝。
  路径键使用 POSIX 表示，保持跨平台清单一致。
- 对已有对应源图保留证据的单位，MISSING_TEXT/TEXT_MISMATCH 等保留为可编辑性诊断，
  不再把必要内容的待审状态覆盖为失败；其他未保留单位的失败仍会传播。
- 正文中的 sym、altChunk、VML/OLE、域、内容部件、未支持特殊字符/文本框/注释引用，
  以及非图片 DrawingML 等当前无法完整评分的元素，明确报告 UNSUPPORTED_BODY_CONTENT。

新增 15 个回归（缺 seal 项、源图降级与未保留的其他文字、未支持正文元素）。
修复前 14 failed / 1 passed，修复后目标 76 passed；独立检出全量 579 passed。
Ruff、mypy（61 文件）、两个既有校验器均通过。没有改写旧封存数据或重新调用识别模型。
新提交仍等待外部 Codex 审核，不以本报告代替审核通过。

## 第五轮：reviewed 基础包、Word 文本控制与链接图片

审查版本 52878d4，修复三项反馈，并覆盖关联的表格段落边界。

- evaluate-only 和 import-reviewed 共用完整基础包验证：五个必需文件必须受 seal 保护，
  并验证输出、参考、输入和选页的跨文件身份一致性。导入不能重新封存一个未经验证的基础包。
- 正文与表格从实际 Word run 节点读取文本：tab、br、cr 转为对应控制字符，
  页/列换行也保留不同控制字符。raw 不忽略，light 仍仅按既定规则去空白；
  格式属性中的 tab stop 不当成文本制表符，表格多段落不直接拼接掉边界。
- a:blip 的 r:link 形式当前明确报告 UNSUPPORTED_LINKED_IMAGE，
  包内链接也不能绕过绘图计数；外链仍由原有关系检查拒绝。

新增 17 个回归：基础包缺项/身份冲突、正文和表格控制字符、格式 tab stop、
链接式 blip 及表格段落边界。初始16例15 failed / 1 passed，修复后目标93 passed；
独立检出全量596 passed，Ruff、mypy（62文件）、两项既有校验器通过。
旧封存包未改写，新提交继续等待外部 Codex 的最新版本审核。

## 第六轮：OPC、隐藏内容与布局表

审查版本 7d21ecd，修复四项反馈。

- 在评分前要求 Content Types、根关系和主文档存在，并验证主文档关系、内容类型和文档根。
- 从实际 styles 关系目标加载样式，解析默认/段落/字符样式继承和直接 vanish 覆盖；
  隐藏内容明确报告 HIDDEN_CONTENT，未使用的隐藏样式不造成误报。
- 非数据布局表不进入数据表拓扑分母；已有同锚点文字时使用该参考，否则以确认 cell 文字构造
  独立文字参考，计入文字覆盖。实际表中错误文字仍可检出，不重复计入数据表 cell 指标。
- 页眉页脚与正文复用未支持可见节点清单，sym/noBreakHyphen 等不再遗漏。

新增17个实际 DOCX 回归，含不完整OPC、直接/继承隐藏和显式显示覆盖、未使用样式、
布局表正反例、非正文符号、非默认样式部件路径。目标110 passed；独立检出全量613 passed。
Ruff、mypy（63文件）、两项既有校验器均通过。原封存证据与本地其他未提交工作未改写。
新提交继续等待外部Codex审核，未以本地通过代替合并授权条件。

## 第七轮：不确定单元的输出归属、额外表格与图片类型

审查版本 b182a8f，三项反馈均补实际 DOCX 回归。

- uncertain 的文字/表格/公式不进入精确分母，但其有来源绑定的对应输出被登记为未评分内容；
  不以全局豁免掩盖无来源的额外文字、数学节点或表格。必要内容及结构状态明确待审。
- 在预期内容评分后核对全部实际表格归属，未被参考内容或已验证图片绑定认领的表格报
  UNALIGNED_TABLE，空但有边框的额外表格也能检出。
- blip 的关系必须是标准 image 类型，目标部件 Content Type 必须与实际解码格式一致；
  错关系类型、非图片类型、错图片类型和缺失类型均拒绝，不只看目标字节哈希。

新增11个回归，修复前9 failed / 2 passed，修复后目标121 passed。
独立检出全量624 passed，Ruff、mypy（64文件）、两项既有校验器通过。
未改变冻结标注或历史输出。新提交仍需完成外部Codex审核。

## 第八轮：OMML 可见性、结构降级与布局包装网格

审查版本 c3d4147，三项反馈均补回归。

- 可见性解析同时处理 w:r 和 m:r 的直接/继承隐藏属性，显式显示覆盖也适用；
  不能先丢弃数学运行属性再把不可见公式当成正确内容。
- 先验证源图保留，再判定结构降级。纯源图替代公式/数据表时保留完整分母及 fallback_count，
  内容和结构待审，可编辑性仍独立显示不足；尚有错误可编辑结构时仍判 FAIL。
- 显式非数据布局表按确认网格核对额外结构，但不进入数据表指标；隐式包装仅支持明确无边框、
  单位跨度、顺序填充的形态，只允许多行最后一行必要的尾部补位，拒绝额外空行/空列或可见边框。

新增13个回归（OMML隐藏/覆盖、公式/表格图片替代与保留错误结构、布局网格正反例）。
修复前9 failed / 4 passed；修复后目标134 passed，独立检出全量637 passed。
Ruff、mypy（65文件）、两项既有校验器通过。旧封存输出未重算或改写，新提交待外部审核。

## 第九轮：实际写入的图片与非正文控制字符

审查版本 d930ab6，两项反馈修复。

- 图片来源清单在 picture() 成功实际写入时登记，不再根据 inline_parts 预估。
  同时含 OMML 和 asset_id 的参考裁剪不冒充输出图片；整块图片覆盖旧内联元数据也不会重复登记。
  图片读取、哈希与实际写入使用同一份字节，原参考裁剪仍保留。
- 非正文 story 使用同一 Word 文本读取器且不删除控制字符；纯 tab/br/cr 明确拒绝，
  paragraph tab-stop 格式属性仍不当成文本内容。

新增11个回归，覆盖真实 recover→finish 的三个公式/图片写入分支及两种story的控制字符/格式属性。
初始10例7 failed / 3 passed，修复后目标145 passed；独立检出全量648 passed。
Ruff、mypy（67文件）、两项既有校验器通过；未调用识别模型或改写旧证据。新提交继续等待外部审核。

## 第十轮：全部部件类型、表格隐藏样式与重复关系

审查版本 2c7e5bd，三项反馈及非法 MIME 形式补回归。

- 除 Content Types 声明本身和目录条目外，每个实际 OPC 部件必须有类型声明；
  关系部件必须使用标准关系内容类型，引用的 Word 部件必须匹配标准类型，媒体主/子类型须有效。
- 可见性加入实际使用表格样式的 run properties 和继承链；支持直接显示覆盖，
  未使用的隐藏样式不误报，暂不支持的条件隐藏样式明确拒绝。
- 每条期望关系只匹配一次，重复预测仍保留在精度分母，不能重复增加正确数或覆盖数。

新增15个回归；最初14例11 failed / 3 passed，另补非法类型形式反例。
独立检出最终663 passed，Ruff、mypy（68文件）、两项既有校验器通过。
旧封存证据保持不变，新提交仍需最新外部审核明确通过后才能合并。

## 第十一轮：多个公式、定位制表符与关系标识

审查版本 82c4ea3，三项反馈及关系命名空间/合法XML注释补回归。

- 同段落多个公式按未消费节点做一对一结构匹配，支持冲突候选的重新分配。
  重复参考不能复用一个节点，额外节点仍报错；未支持/不确定公式各自认领剩余节点，
  不能凭同段落其他公式的存在冒充自身内容保留。
- ptab 由正文/非正文共用的未支持可见节点策略明确拒绝。
- 每个关系部件在构建映射前检查命名空间、XML ID语法及ID唯一性；普通XML注释不当成关系记录。

新增20个实际DOCX回归，覆盖多个正确/错误/重复/缺失/额外公式、未知与不确定公式共段，
定位制表符、重复/非法关系ID、错误命名空间及合法注释。
最终独立检出683 passed，Ruff、mypy（69文件）、两项既有校验器通过。
旧封存数据未改写；新提交继续等待外部审核，未提前合并。

## 第十二轮：自动编号与重复内容类型声明

审查版本980f447，两项反馈修复。

- 当前明确拒绝自动编号/项目符号，检查直接numPr、段落样式继承、表格样式及已实例化编号关联；
  非正文也复用检查。显式numId=0关闭编号和未使用样式不误报。
- 构建内容类型映射前检查Override PartName及Default扩展名唯一性，Default按大小写无关匹配。
  相同重复声明和冲突声明均不能通过字典覆盖隐藏。

新增13个实际DOCX回归，修复前11 failed / 2 passed；目标190 passed。
独立检出最终696 passed，Ruff、mypy（70文件）、两项既有校验器通过。
旧封存数据保持不变；新提交继续等待外部审核明确通过后合并。

## 第十三轮：可渲染图片容器与必需关系属性

审查版本b6fa7fd，两项反馈及相关显示边界补回归。

- 图片哈希之外，要求完整受支持的inline图片容器：标准picture graphicData、尺寸、非视觉属性、
  图片填充及一致的形状变换；裸/游离blip、缺尺寸/结构均拒绝。
- 当前支持完整矩形内联图片及常见Office DPI注记；浮动anchor、裁切、透明/隐藏、未支持效果等
  不能冒充完整源图保留，明确失败而非根据ZIP中的字节认领。
- 分派关系类型前要求合法非空绝对Type URI、非空Target和有效TargetMode。
  自定义绝对URN关系仍允许，不访问或解析远端资源。

新增20个实际DOCX回归，修复前18 failed / 2 passed；目标210 passed。
独立检出最终716 passed，Ruff、mypy（71文件）、两项既有校验器通过。
旧封存数据未改写，新提交继续等待外部Codex审核。

## 第十四轮：兼容分支与 OMML 命名空间

审查版本e7e81c6，两项反馈修复。

- 当前明确拒绝主文档、引用story及所加载样式/编号中的AlternateContent，
  不拼接Choice和Fallback，也不猜测Office能力选择分支。
- OMML规范化前验证节点命名空间及允许的Word运行属性位置；
  不将同名外来节点当作数学节点，文本节点也不能隐藏非法元素子节点。
- 合法XML注释被忽略但其后的文本保留，Word和数学文本均合并正确的文本片段。

新增13个回归，覆盖正文/非正文/样式兼容分支、外来公式节点、合法Word属性、注释和简单文本结构。
最终独立检出729 passed，Ruff、mypy（72文件）、两项既有校验器通过。
上轮网络错误导致的两条旧讨论已成功补标解决。旧封存输出不改写，新提交仍等待外部审核。

## 第十五轮：修订视图不确定的内容

审查版本46d5014，修复最终视图隐藏内容仍参与评分的问题。

- 在读取正文、公式、图片前拒绝待处理修订容器、移动范围、格式/表格变更及修订删除文本。
  所加载样式/编号和引用story应用相同检查，不猜测最终/原始/显示标记视图。
- 只开启trackRevisions、尚无修订记录，或带普通rsid保存属性的文档不误报。
  reviewed文件应在Office中先接受或拒绝修订，再导入评分。

新增9个实际DOCX回归，修复前8 failed / 1 passed。最终独立检出738 passed，
Ruff、mypy（73文件）、两项既有校验器通过。旧封存证据不改写，新提交继续等待外部审核。

## 第十六轮：缺省表格编号与旧式水平合并

审查版本33006bd，两项反馈均已复现并修复。

- 表格未显式指定样式时，编号检查使用缺省table样式，并沿basedOn解析；
  条件表格样式编号也不能被遗漏。显式普通样式及段落numId=0覆盖仍保持有效。
- 当前明确拒绝旧式hMerge，不再将Word中的水平合并显示当成两个独立单元格通过。
  已支持的gridSpan/vMerge继续按原逻辑检查。

新增7个实际DOCX回归，修复前5 failed / 2 passed；目标20 passed。
独立检出最终745 passed，Ruff、mypy（74文件）、两项既有校验器通过。
保留已有Starlette弃用警告，不改写封存证据；新提交继续等待外部审核。

## 第十七轮：不确定关系的评分与续格内容

审查版本138ff65，两项反馈核实后修复。

- 将非confirmed关系真值单独传入评分器，按已绑定端点和关系类型逐条认领预测；
  每条uncertain边最多排除一个预测，保留eligible/not_scored/uncertain计数，
  precision数值以scored_count为分母。重复、无关或未绑定预测不借此豁免。
- 垂直合并续格除文字外，也拒绝制表符、换行、数学节点和图片；
  合法空续格仍可通过，不把存在载荷的续格静默丢弃。

新增12个实际DOCX/真值组合回归，相关目标42 passed。独立检出最终757 passed，
Ruff、mypy（75文件）、两项既有校验器通过，保留原Starlette弃用警告。
仅修正新评分行为，旧封存报告不重算、不改写；最新提交仍须外部审核通过。

## 第十八轮：图片自身隐藏属性

审查版本f203a40，补齐pic:cNvPr的hidden检查，与wp:docPr一致。
属性缺省、0、false保持可见；1、true等非可见值明确报HIDDEN_CONTENT，
不可凭媒体哈希认领隐藏图片。新增5个实际DOCX回归，修复前2 failed / 23 passed。
独立检出762 passed，Ruff、mypy（75文件）、两个既有校验器通过。
用户已取消定时跟进，改为直接完成审核、合并与安全分支清理；产品验收边界不变。

## 第十九轮：可能隐藏内容的底纹与高亮

审查版本8df1afc，复现文字/公式与运行底纹同色仍被评分的问题。
当前独立读取器不解析主题、图案和背景混合的最终对比度，因此明确拒绝已使用的
实色/图案底纹与高亮，而非声明这些格式可见。检查直接运行（含OMML）、段落/单元格、
表格属性、默认格式、已用样式继承链及条件表格样式；未用样式与无底纹不误报。

新增11个实际DOCX回归，修复前9 failed / 2 passed，修复后11 passed。
独立检出773 passed，Ruff、mypy（76文件）、两个既有校验器通过。
该支持限制不等同于视觉验收；历史证据不重写，继续等待最新提交外部审核。

## 第二十轮：隐藏空白与全量不确定关系

审查版本30022f2，两项反馈修复。
隐藏载荷检测不再strip，纯空格也不可从不可见运行中取出后计为精确正文。
coverage_complete不再因存在uncertain边整体失效：逐条匹配的预测仍不评分，
其余重复/无关预测继续评分，即使没有任何confirmed边。

新增6个回归，修复前3 failed / 3 passed，修复后独立检出779 passed；
Ruff、mypy（77文件）、两个既有校验器通过。旧封存证据保持不变。

## 第二十一轮：颜色、动态页码、story根与书签契约

审查版本0fc7889，四项反馈修复。

- 当前仅支持黑色/auto前景及缺省/显式白色页面；已用其他颜色、主题色、复杂页面背景
  明确不支持，不以XML中存在文字推断可见。白字白底、黑字黑底均有实际回归。
- 动态pgNum明确拒绝，正文与已引用页眉页脚一致，不猜测Word重分页后的页码。
- 按关系类型校验hdr/ftr/footnotes/endnotes根元素及命名空间。
- 来源书签要求合法ID、start/end配对及顺序、ID/名称唯一；缺失或重复明确失败。
  修正三个合成夹具缺少bookmarkEnd的问题；复制段落场景仍保留，但更早以
  INVALID_BOOKMARK拒绝，不再等到DUPLICATE_SOURCE_BLOCK。

新增24个回归，最初22项中17 failed / 5 passed，页面背景补充回归修复前1 failed。
最终独立检出803 passed，Ruff、mypy（78文件）、两个既有校验器通过。
系统临时工作树清理后已重建并重新完成验证，真实历史证据未修改。

## 第二十二轮：批注与部件引用

审查版本8a33e6a，三项反馈修复。
拒绝批注标记和非空comments部件；空批注集合不误报。页眉页脚引用逐项验证r:id和
精确关系类型，不接受悬空、图片或相反story关系。样式/编号根名和命名空间必须正确。
相关样式/story关系分派使用完整标准URI，不依据任意URI的最后一段猜测类型。

新增19个实际DOCX回归，修复前14 failed / 5 passed；最终独立检出822 passed，
Ruff、mypy（79文件）、两个既有校验器通过。旧证据和未提交的前置成果不变。

## 第二十三轮：段落外公式与行级底纹

审查版本7f50a7b，两项反馈修复。
当前来源绑定以Word段落为单位，段落外oMath明确报UNSUPPORTED_BLOCK_MATH，
不允许显示公式绕过节点评分；段落内oMathPara对照保持可评分。
表格行tblPrEx背景属性纳入现有可见性检查，与表格/单元格规则一致。
新增6项回归，修复前4 failed / 2 passed；独立检出828 passed，
Ruff、mypy（80文件）、两个既有校验器通过，原工作与证据不改写。

## 第二十四轮：隐藏行与强制文档保护

审查版本b738145，两项反馈及同类样式入口修复。
隐藏表格行在直接行属性和已用样式继承/条件属性中均不可用于评分；关闭隐藏的对照保留。
读取准确关联的settings部件并验证根元素，启用documentProtection时明确报不支持，
不能从XML可提取性推断Word可编辑性；缺省/关闭enforcement不误报。
新增15项回归，直接入口修复前6 failed / 5 passed，样式入口另有3 failed；
最终独立检出843 passed，Ruff、mypy（81文件）、两个既有校验器通过。

## 第二十五轮：内容锁定与扩展文字效果

审查版本9b9056f，两项反馈修复。
内容控件contentLocked/sdtContentLocked明确拒绝；无锁、unlocked和仅限制删除控件的
sdtLocked不误判为内容不可编辑。已用格式中的非Word标准命名空间扩展节点明确不支持，
覆盖w14文本填充/轮廓在正文、公式、已用样式和默认格式中的不可见效果。
新增13项回归，修复前10 failed / 3 passed；独立检出856 passed，
Ruff、mypy（82文件）、两个既有校验器通过。原证据不改写。

## 第二十六轮：写保护、日期与部分关系标注

审查版本425dcff，三项反馈修复。
settings含writeProtection时明确不支持；动态day/month/year长短格式节点与动态页码
同样拒绝，不猜测显示值。关系标注不完整时仅评分可由已登记边认领的预测，额外未知边
保留unknown/not_scored计数；完整真值的额外预测、已知边重复预测仍会失败。
原完整合成夹具显式登记coverage_complete=true，部分覆盖用例自行设false，场景未移除。
新增18项回归，修复前15 failed / 3 passed；独立检出874 passed，
Ruff、mypy（83文件）、两个既有校验器通过；既有封存报告不重算。

## 第二十七轮：实际图片缩放与可编辑性错误

审查版本356e0a1，两项反馈修复。
图片显示宽高至少1 PDF point，且与像素宽高比例偏差不超过2%；按实际DrawingML尺寸
认领图片时，宽高分别不得小于登记来源框的25%，避免完整哈希掩盖极端缩小/压扁。
这是排除明显不可用输出的工程门槛，不等同于人工视觉可读性验收。
原应可见合成图片显式设为1英寸，保留所有缺失/替换/降级场景；真实历史输出不改写。
保护/内容锁定错误直接令editability_status=FAIL，包括无可评分单元的情况。
新增10项实际回归；最终独立检出884 passed，Ruff、mypy（84文件）、两个既有校验器通过。

## 第二十八轮：降级用途与图片像素异常

审查版本eb5c6dc，两项反馈修复。
仅显式fallback图片可保全缺失正文/公式/表格；普通配图只可保全其自身图内文字，
不因来源框几何覆盖而替代其他必要内容。Pillow解压炸弹异常在全部图片读取路径
统一转换为IMAGE_PIXEL_LIMIT，输出失败结果而非向用户抛回溯。
新增8项回归，修复前5 failed / 3 passed；通过降低测试像素阈值触发真实异常，
不创建巨型图片。独立检出892 passed，Ruff、mypy（85文件）、两个既有校验器通过。

## 第二十九轮：可忽略扩展包装器

审查版本5569ee9，修复未实现MCE处理却从扩展包装器提取必要内容的问题。
提取前检查Word/公式/图片载荷的祖先命名空间，未知扩展包装器明确报
UNSUPPORTED_MARKUP_COMPATIBILITY；ProcessContent不在当前支持范围，亦不猜测处理。
仅声明Ignorable而未使用扩展元素的普通文档继续接受，既有Office图片DPI注记不受影响。
新增5项回归，修复前4 failed / 1 passed；独立检出897 passed，
Ruff、mypy（86文件）、两个既有校验器通过，原始证据不改写。

## 第三十轮：极小字号验收边界

审查版本1813f40，修复0.5pt格式仍可凭完整文字通过的问题。
已用直接/样式/默认格式中的sz和szCs低于6pt（12半磅）或非法值明确报
UNSUPPORTED_FONT_SCALE。该工程验收门槛不修改原始字号，不冒充人工可读性通过；
如真实原稿本就小于门槛，也保留原文并报告不支持，而非静默放大。
新增12项回归，修复前10 failed / 2 passed；独立检出909 passed，
Ruff、mypy（87文件）、两个既有校验器通过，历史证据保持不变。

## 第三十一轮：运行偏移与绑定缓存

审查版本86da76c，两项反馈修复。
已用直接/样式/默认格式中的非零运行position明确不支持，正文与公式一致，
不从页面外字形提取内容后宣称可见。含dataBinding的内容控件明确拒绝，
不把可能在Word打开时被同步替换的sdtContent缓存当作最终内容。
新增10项回归，修复前5 failed / 5 passed；独立检出919 passed，
Ruff、mypy（88文件）、两个既有校验器通过，原始输出保持不变。

## 第三十二轮：水平压缩、行裁剪与WordML父链

审查版本ea404c9，三项反馈修复。
已用w:w非100%或fitText格式明确不支持，防止正常字号被水平压至不可读；
精确表格行高明确不支持，auto/atLeast仍可接受，直接/样式属性共用检查。
验收载荷的WordML父链按受支持段落、表格、普通控件结构验证，属性容器中非法嵌套
段落/运行/文字/公式/图片不再被后代查询认领；数学内部继续交给独立OMML验证器。
新增21项回归，修复前14 failed / 7 passed；最终独立检出940 passed，
Ruff、mypy（89文件）、两个既有校验器通过，原始证据不改写。

## 第三十三轮：字符间距与大小写显示效果

审查版本d9bb9f5，两项反馈修复。
已用运行级非零spacing明确不支持，避免字形重叠或被推离页面；段落行距属性不混淆。
启用caps/smallCaps明确不支持，避免严格文字比较忽略显示大小写变化。
直接、正文/OMML、默认与已用样式共用规则，零间距/关闭效果对照保持通过。
新增28项回归，修复前16 failed / 12 passed；独立检出968 passed，
Ruff、mypy（90文件）、两个既有校验器通过，原始证据不变。

## 第三十四轮：段落框、包根目标与评分器指纹

审查版本a8840d1，三项反馈修复。
段落framePr明确不支持，直接/已用样式共用检查；不从页面外定位段落声明内容可见。
内部关系统一规范化包根绝对目标及相对路径，接受/word/...合法目标，同时拒绝越过
包根的..路径及非部件URI。评分器指纹包含acceptance模块、当前product_acceptance入口
和结果Schema，使用仓库相对路径键，不再遗漏结果组装/契约变化。
新增9项回归，修复前8 failed / 1 passed；独立检出977 passed，
Ruff、mypy（91文件）、两个既有校验器通过；旧封存评分不改写。

## 第三十五轮：段落行距裁剪

审查版本203407d，修复精确段落行高仍被忽略的问题，并覆盖同类极小自动行距。
段落spacing的exact规则、低于单倍的auto行距及非法值明确不支持；正常auto与
可按内容增长的atLeast保留。正文/OMML段落、默认和已用样式统一检查。
新增16项回归，修复前8 failed / 8 passed；独立检出993 passed，
Ruff、mypy（92文件）、两个既有校验器通过，原始证据不变。

## 第三十六轮：段落/表格缩进与浮动定位

审查版本1efdb66，两项反馈及同类表格缩进入口修复。
浮动tblpPr明确不支持；段落twip缩进和普通表格缩进受文档各节可用宽度四分之一
且最多90pt的保守上限约束，悬挂缩进还不超过最小页边距。负值/未支持字符单位
明确拒绝，正常小缩进不误报；原输出位置不自动调整。
新增16项回归，初始14项中5 failed / 9 passed，表格缩进追加1个失败回归。
独立检出1009 passed，Ruff、mypy（93文件）、两个既有校验器通过，原始证据不变。
