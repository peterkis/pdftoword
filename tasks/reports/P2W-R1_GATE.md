# R1-08 首轮实物核验结论

2026-09-19。基线 `4e2c952`，工作分支 `codex/r1-07-reconstruction-chain`。

**R1-08 核验工作已执行，保留明确缺陷；不签完整产品视觉接受或发布接受。** 用户新安装 Word 后，原目标平台环境阻断已解除。代理依照用户授权完成 Mac 桌面检查；本报告中的桌面核对是代理实测，不冒称用户本人签字。

## 修改与交付

- `acceptance.py`：重开 DOCX，逐来源块核对书签范围内实际文字、换行、制表符、嵌入图片和 hash；核对 source-map 的页码/bbox、唯一性和完整覆盖。错换非空书签的反例会失败。
- Flow 仅对明确人工确认、同页连续的 caption_of 范围保持连续分页；不推测归属、不改正文。native25 的三行图注经源图确认后通过现有 overrides 生成独立 reviewed 产物，自动反例保留。
- 8 个真实物理页的 source/before/after、IR、RenderPlan、source-map、版本/hash、两种渲染平台证据和 [R2 backlog](P2W-R1_R2_BACKLOG.md) 已建立。没有将合成页计入真实范围。
- R1-04/R1-06 已提交 `4e2c952`；后续 R1-07/R1-08 工作仍未提交，未 push/merge。

## 实际验证范围

| 标签 | 来源 | Word 实测与结论 |
| --- | --- | --- |
| scan46-47 | 真实扫描图文 2 页 | before/after 各 2 页；after 的第 47 页导语和图在同页，中文可见。实际插入/删除正文、保存、关闭重开、书签定位成功；第 46 页仍全页图像回退 |
| scan15-16 | 真实扫描文字、表格图、故事图 2 页 | before 1 页、after 2 页；表格图与题 3 邻接，故事图在故事之后。表格仍保图，不宣称可编辑 |
| scan40-41 | 真实中英扫描正文 2 页 | before 1 页、after 2 页；中文可见，文字与图片无新增丢失；原页码进入正文流等问题仍在 |
| native4 | 真实原生电子列表 1 页 | Word 中 before/after 均 2 页；列表标签位于正文之后，视觉拒绝。此前 LibreOffice before 3 页不能套用于 Word |
| native25 | 真实原生电子图文 1 页 | before/after/reviewed 各 2 页；自动输出仍有碎片成段、图注拆页。reviewed 的完整图注与图同页，人工确认路径有效，自动识别缺陷未消失 |

每个来源作业不超过 2 页，符合最多 3 页限制。native25 是原生文字加实际图形的自然图文混合页。未运行新的模型推理。

## Microsoft Word 证据

版本 **16.113.1 / 16.113.26091740**。11 份独立 DOCX 副本实际经 Finder “打开方式 → Microsoft Word”载入，在 Word 的兼容性模式下操作；无修复文档提示。全部经原生“打印 → 保存为 PDF”得到 Word 实际渲染证据，不再以 LibreOffice 代替 Word。

scan46-47 after 的编辑测试在本地副本插入 `R1-08 WORD EDIT PROBE`，图片随文下移；保存插入状态后删除测试段落、再保存、关闭和重新打开。恢复后的完整文字与原 DOCX 一致，嵌入图片字节一致；17 个来源范围逐块校验成功。Word 书签对话框列出 17 个 p2w 书签，实际“定位”选中了 source-map 指定的源页码文字范围。

5 份 after 加 native25 reviewed 的 Word PDF 文字层与 DOCX 文字完全一致（仅忽略排版空白）。5 份 Legacy PDF 的部分汉字提取为 Unicode 康熙部首兼容字符；诊断中仅映射该区段后逐字相同。原始差异和 PDF 均保留，没有修改原文或用归一化掩盖正式原始比较。

打印流程提示页边距较小；代理查看 after 实际 PDF 未见新增裁切，保留告警。该证据不等于物理打印机接受。Word 自动保存为关闭，标题显示保存到“我的 Mac”，没有向在线 Word 上传样本。

## 几何、来源与关系核对

- 对 R1-03 46–47 页的 Monkey/PP 真实封存叠框重新校验 hash 并逐图查看：坐标帧与源页对齐，Monkey 宏框包住正文/整幅插图；PP 图内细框不应拿来替换整图。
- 第 46 页输出仍为图像回退，缺独立可编辑内容锚点；第 47 页严格支撑 7/16，另 9 块保留标点、下划线及行片段差异。**实际仲裁继续 ABSTAIN；正向真实采用证据仍未建立。** 这是核对后的不采用结论，不降低门槛、不手指定 Monkey 胜出，也不把视觉上框合理升级为自动绑定通过。
- 11 份原有 before/after/reviewed 共 313 个来源范围的实际文字/图片逐块对应正确；另有 Word 保存重开后 17 个范围的独立验证。
- 第 47 页插图属于形容词讲解，位于导语与练习标题之间，不伪造它属于某道编号题。native25 图 1-5 三行图注的明确关系有源图、overrides、源 block content 不变证明以及 Word 同页渲染。
- 已支持的小型表格/OMML 能力仍由 R1-05 公共 API 探针和专项回归证明，不能外推成这 8 页全部具备可编辑复杂结构。精确 text/image 范围审计遇到未支持数学内容会明确拒绝。

## 最差页的三项实际变化

46–47 页的 Word 对照确认：固定 A4 改为来源纸张尺寸；整页回退图按可用纸张容纳；第 47 页导语与插图不再跨页分离。这些包括分页/图文组合变化，不只是颜色或字号改变。图注 reviewed 版本还明确消除了图 1-5 图注拆页。

这些局部改善不抵消 native4 顺序错误、native25 原生行碎片及自动图注失败。全局视觉接受保持拒绝/待专项修复，R2/R3 的责任、验证要求见 backlog。

## 自动检查与接受矩阵

最后代码检查仍为 `checks/quality-source-ranges/`：**1567 passed**；Ruff、task_catalog、model_baseline、schema_samples 成功；相关 mypy 成功。全仓 mypy 的 2 个旧错误仅在 `tmp/docx-demo/r1-05/trace-public-calls.py:15–16`，quality 整体退出 1。没有加 ignore，也不把整体称为全绿。本轮新增 Word 桌面/文件证据，没有产品代码变更；未为了桌面操作重复整套测试。

| 条款 | 核验结论 |
| --- | --- |
| AC01 | 8 页 before/after 及 manifest 完整；Word 补充文件、版本/hash 和实际 PDF 可交付 |
| AC02 | 已核对无新增静默漏文/图资产替换；存在视觉缺陷，依条款不签整体视觉接受；人工图注修正独立标明 |
| AC03 | 真实 Monkey 响应入候选/仲裁链、DocVortex 公共执行、Flow 产物均有证据；Word 已实测。真实 Monkey 正向采用仍为已知缺口，不强求赢家 |
| AC04 | 六类复用均有必需 R2/R3 负责模块和任务，收益、损失、拒绝边界有记录，未挂可选 E2 了事 |

用户授权的 R1-08 核验范围已形成结果；此结论不关闭 R2/R3 缺陷，不授予发布接受，不自动开始 R2。

## 证据与保留

私有根 `tmp/docx-demo/r1-08/`：原 8 页 ZIP、v2 ZIP、全部自动/修订作业及历史失败均保留。`word-review/` 保存 11 份独立 DOCX、插入状态 DOCX、11 份 Word PDF、对应 PNG、输入 hash、实际渲染/桌面观察/几何复核/书签保存后校验收据。最终 Word 交付包与完成审计单独生成，旧包不覆盖。

Word 安装前的阻断报告保存在 `word-review/gate-before-word.md`，其缺 Word 状态仅代表历史。此前 Pages 自动保存过 iCloud 的事件继续记录于旧报告/receipt：已经移回本地，但云端同步历史无法确认。Word 本轮使用本地副本，原始 source/auto/reviewed 封存文件未变。
