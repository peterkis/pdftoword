# FIX-03：B分类图关系恢复

2026-09-22；HEAD `e1eef44f8cd45b08e081ade3603cc31cbf9db25e`，未提交。按批准的两片串行计划，本文记录第一片退出条件；B表1重复仍待FIX-04。

## 修改

新增有界分类图归属：实际闭合描边框、直线连接及唯一单侧箭头端部组成连通图；复用精确字形绑定。原图只裁剪、不重画。路径端点只能按实际描边半径解释，非等比例变换不猜描边宽度；未知绘制、歧义、框外/跨框文字或重复字形拒绝归属。

图内候选及run→字形→框、带方向连接映射保留；统一输出一个figure，明确 `native_diagram_image_fallback`，source-map及图片记录fallback为true。图注复用caption_of与shared_row，技术标准图号不挂题号。writer保留原公式/表格图片降级语义。

## 验证

真实源为WS/T306—2023第4–5物理页。144个原生run全部各归属一次（58、86）；图1的4框、6条有向连接、20个run已完整保留。其他文字与旧输出扣除图内重复文字后完全一致，标注未进入转换输入。当前仍4个Word输出页，上方表格重复未关闭。

Microsoft Word实际打开、导出PDF并检查全部4页：图1内部关系完整，只出现一次，图注在同页；字体、页眉/页码位置和段距差异仍LIMITED。打印时出现第1、2节页边距提示，保留实际PDF核验，未发现图1裁切。

独立编辑副本插入 `B03EDIT`，保存、关闭、重开成功。仅增加该标记；媒体哈希、83个来源范围及其中的实际图片/正文保持。自动原件未改。

新增分类图测试先因缺少实现失败，最终分类图/字形/网格定向测试37 passed；ruff通过；全仓mypy通过（219源文件）。实施阶段末全量回归记于FIX-04。最终代码重新读取的两页observation与已在Word核验作业完全一致。

## 证据与边界

私有根：`tmp/docx-demo/f3-03-fix-03-20260922/`。
`baseline.json`/`baseline.diff`/`preservation.json`保存原始工作区和证据基线；`cases.json`记录实际CLI、退出码0、耗时及资源；连接拒绝钩子无连接尝试，模型0（不代表Word后台网络0）。
`B/auto.docx`、`B/word-render.pdf`、`B/word-page-1.png`至`4.png`、`B/body-edit.docx`及`edit-reopened.png`为直接产物；`verification.json`、`audit.py`及检查日志为来源和编辑核验。

第一片局部退出条件已满足，可以串行实施FIX-04。图内文字仍不可编辑，用户接受PENDING；不宣布B整页质量通过或进入F4-01。
