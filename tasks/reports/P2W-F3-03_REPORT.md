# F3-03：受限支持范围与实际 Word 验收

**结论：验收记录已交付，存在发布阻断；不建议进入 F4-01/Beta。用户接受 PENDING。**
本轮只验证 `fidelity-v3.1`，未修改转换代码、切换默认、提交或推送。

## 修改与基线

新增本报告与私有验收产物；原工作区修改完整保留。
HEAD：`e1eef44f8cd45b08e081ade3603cc31cbf9db25e`，使用未提交的 F1-04/F2-03 工作区。
`baseline.json` 保存完整既有工作区文件哈希、依赖/环境；`baseline.diff` 保存未提交差异。

- 7 个去重物理源页，6 个原生页＋1 个扫描结果回放页；生成 6 组自动作业。
- 4 个新增页仍为原定 WS/T 305—2023 第 6、7、24、26 页，没有按结果换样。
- 本地历版样本清单只登记该文档第 8–9 页，F1/F2 未使用新增四页修代码；但 batch4 明确保留未知历史参与状态，因此四页均记 `PRIOR_DEBUG_UNKNOWN`。**已确认独立新增页为 0，未确认新增页为 4**，不宣称新增盲测资格。
- 旧项目清单人工审核记录与 batch4 `DRAFT_AGENT_VISUAL` 分别保留；本轮是 agent_visual 核验，没有数据集导入、family/split 调整或人工签署。
- 全部正文、图片、标注和逐对象核对信息只保存在私有目录 `tmp/docx-demo/f3-03-20260921/`。

## 支持范围与逐页结果

VERIFIED 仅指记录中的具体检查范围已有代理实测，不代表产品/用户接受。
LIMITED 表示存在已知限制；UNSUPPORTED 表示目标能力不可用；NOT_COVERED 表示没有对应真实覆盖。

|业务类别|native|scan|mixed|
|---|---|---|---|
|试卷|NOT_COVERED|LIMITED：仅 A 的冻结识别结果重建，未做新 OCR|NOT_COVERED|
|合同|NOT_COVERED|NOT_COVERED|NOT_COVERED|
|技术/操作手册|NOT_COVERED|NOT_COVERED|NOT_COVERED|
|一般文档|LIMITED：六个技术标准页；正文子范围可用，含表格/组合图页有阻断|NOT_COVERED|NOT_COVERED|

技术标准没有计作合同或操作手册。页面含普通图片不自动改记 mixed。

### 七页分轴结论

所有七个选定源页及对应 **16 个实际 Word 输出页**均已完整查看，不报全文准确率或综合分。

|组/源物理页|实际 Word 页|内容|结构/关系|布局|可编辑性|主要影响|
|---|---:|---|---|---|---|---|
|A：数学试卷 1|1–2|VERIFIED|LIMITED|LIMITED|LIMITED|可见文字、14 式及7图/标签核对；第5题题干与图跨页，8/9题题干不在输入；支持分式实际编辑成功|
|B：306—2023 / 4|1–2|VERIFIED|LIMITED|LIMITED|LIMITED|22 个文字块核对；原生行仍为独立段落，一页变两页，页脚作为正文流动|
|B：306—2023 / 5|3–4|LIMITED|UNSUPPORTED|LIMITED|UNSUPPORTED（数据表）|13 块及39格核对；表图后重复文字/线段，分类图节点与箭头散开，关系破坏|
|N1：305—2023 / 6|1–2|LIMITED|UNSUPPORTED|LIMITED|UNSUPPORTED（数据表）|19 块及16格核对；表图后重复所有单元格文本；形式表达式不是数学公式覆盖|
|N2：305—2023 / 7|1|VERIFIED|LIMITED|LIMITED|VERIFIED（正文）|33 块含图中文字核对；两幅完整源图及图注可见顺序正确；正文编辑保存重开成功，显式语义关系和原字体/位置不作通过声明|
|N3：305—2023 / 24|1–4|LIMITED|UNSUPPORTED|LIMITED|UNSUPPORTED（数据表）|8 块及195格核对；三张表图后重复行文字/线段，印刷页码单独占第4页|
|N4：305—2023 / 26|1–3|LIMITED|UNSUPPORTED|LIMITED|UNSUPPORTED（数据表）|9 块及62格核对；附录标题、完整/局部表图及文字重复，真实空字段和合并关系仅在图中保留|

原生材料共核对 104 个参考文字块和 312 个实际单元格；这些是检查范围数量，不是正确率。
图内文字和图片回退不计作可编辑成功；子表达式与所属块/单元格不重复计分。
原文中的可疑拼写、大小写、数字和空字段保留，不以语义常识纠正。

### 发布阻断与受限使用影响

1. **重复呈现内容：B5、N1、N3、N4。** 源图已经包含的文字/线段又被作为独立内容输出；N4 的附录标题也重复。会增加阅读内容、破坏阅读次序和分页，不能仅归为外观差异。
2. **图关系破坏：B5 图1。** 四个分类框、独立文字及连接箭头被拆成纵向对象，已不能保持源图连接关系。
3. **数据表不可编辑：六张源表均没有 Word 单元格拓扑。** 其中包括普通符号/枚举表及复杂合并表；正文能编辑不代表表格能编辑。A 的两张无边框图组排布表不能充数。
4. **未达原页布局目标。** A 1→2页、B 2→4页、N1 1→2页、N2 1→1页、N3 1→4页、N4 1→3页。字号/字体替换、段落间距、图表位置和页脚流动均有影响；N2 页数相同也未判布局 VERIFIED。

没有确认文件损坏或新增外发。平均结果不抵消上述内容/关系阻断。

## 实际 Word、编辑与验证

### 环境与显示

macOS **26.6.2 / 25G83**；Microsoft Word **16.113.1**。Windows NOT_RUN。
本机有 Arial、Arial Unicode、Songti、Times 等字体文件，未复制或分发字体。
候选默认输出 Arial / Arial Unicode MS；原生源中的 SimSun 使用本地替换，SimHei、部分子集命名字体记录 reflow risk；扫描源字号仍为推断。
默认 body 字号记录：A 11pt；B/N1/N2 10.45pt；N3/N4 9pt。没有逐页调参。

六份 DOCX 均实际在 Word 打开，并通过 **打印 → PDF** 导出；没有选实体打印机、没有使用官网或其他渲染器。
PDF 再以本地 PDFium 转图供完整逐页核对，不冒充新的 Word 编辑证据。
所有导出出现“小页边距可能截断”警告，已记录环境因素；警告本身不等于内容截断。

纸型实测：A 源虚拟页约595.28×862.41pt，Word PDF约595.30×862.40pt；其他源页约595.276×841.890pt，打印PDF为595×842pt。没有据此设定通过容差。
`word-measurements.json` 保存各输出页纸型及可见文字边界；该边界不是 Word 页边距或图片边界。
精确 Word 对象位置及其容差为 NOT_MEASURED/NOT_DEFINED，不能使用源 bbox 代替。

### 编辑保存重开

- **正文**：N2 独立 `body-edit.docx` 在指定正文前插入 `[F3BODY]`，保存、关闭、重开；图文次序仍可见。OOXML复核只有预期文字变化，书签与媒体不变。
- **公式**：A 独立 `formula-edit.docx` 在 Word 内编辑第3题A分式，分子 **12c→13c**、分母 **4a** 保持，保存、关闭、重开。仍有14个OMML对象，另外13式数学文本及普通正文、书签、媒体不变。首次文本定位未进入公式，误插的普通文字已撤销，未当成公式编辑成功；后续在内置公式编辑器中完成并检查实际XML。
- **单元格**：UNSUPPORTED。六张源数据表均没有可编辑数据单元格，不用图组排布表或构造例替代。

上述均为诊断副本，不改变自动原件、不计识别成绩。没有要求 Word 保存后的XML逐字节不变。

### 自动检查与工具限制

|检查/命令|实际结果|
|---|---|
|`PYTHONPATH=. uv run python tmp/docx-demo/f3-03-20260921/generate.py`|退出0；六次既有CLI调用均退出0，socket连接阻断，尝试0；原资产/标注/历史文件集不变|
|`PYTHONPATH=. uv run python tmp/docx-demo/f3-03-20260921/render-word-pdfs.py`|退出0；6份实际Word PDF，16页PNG与文本提取辅助记录|
|`PYTHONPATH=. uv run python tmp/docx-demo/f3-03-20260921/audit.py`|退出0；独立ZIP/OOXML检查及343个真实payload来源范围核验，参考文本存在性仅作辅助|
|正文/公式编辑副本检查|预期编辑成立，其余正文/数学/图片/书签保持；见 edit-verification.json|
|`PYTHONPATH=. uv run python tmp/docx-demo/f3-03-20260921/finalize.py`|退出0；7页去重、6组原auto及Word查看副本、原材料和既有工作区哈希保持|
|既有独立 DOCX reader|A返回 `UNSUPPORTED_TABLE_WIDTH`；原生组返回 `UNSUPPORTED_TEXT_POSITION`。保留原错误；其空结果不能当零内容或质量通过，未修改读取器绕过门禁|
|关键路径回归|转换代码/依赖本轮未改，复用连续会话F2-03的1698项全量＋47项最终定向收据；未声称本轮重新执行|
|`git diff --check`|退出0|

没有CER/总分。去空白字符串匹配只用于找待查看对象，不证明逐字或视觉准确率。
六组新的原始自动作业和查看用副本在本轮核验后仍保持原始哈希。

### 有界性能记录

均为本机CLI本地生成/回放，含启动开销，不含Word人工操作；不是在线推理性能。

|组|可见耗时（秒）|`time -l` 峰值驻留内存（字节）|
|---|---:|---:|
|A|0.317|71,565,312|
|B|1.010|110,460,928|
|N1|0.557|106,381,312|
|N2|0.429|105,562,112|
|N3|1.416|112,787,456|
|N4|0.641|109,314,048|

验收记录生成时总墙钟约26分钟；最终起止以 baseline.json/summary.json 为准，低于6小时上限。
无等待真人操作；Word工具恢复及编辑步骤没有独立计时，标NOT_MEASURED，不估P99或修复成本。
转换代码冻结，修复工时不适用。

## 证据、风险与下一单一修复

私有入口：`tmp/docx-demo/f3-03-20260921/index.html`。
每组 `A/B/N1/N2/N3/N4/` 含源页PNG、auto.docx、word-render.pdf、全部word-pages、OOXML辅助核对及原auto清单。
正文/公式编辑副本和重开截图分别在 N2/A。统一 summary.json 保存逐页结论与4×3支持表；
`scope-and-exposure.json` 记录新增页资格与标注边界；`preservation-final.json` 记录保持性。

环境过程记录：屏幕捕捉曾出现 `-3811`，恢复后完成剩余导出，未将尝试当成功。
第一次PDF保存将路径当作文件名落入旧目录；仅将该次新增PDF移回本轮目录，没有覆盖旧文件，最终原证据目录的精确文件集合和哈希均匹配。

**下一单一修复建议：N1 第6页表2（`p5-figure0`）的原生区域输出归属。**
先解决“同一区域源图＋重复文字/线段”这一内容阻断：以可验证的表区域覆盖证据选择唯一局部渲染策略，
保留所有原生文字候选及来源，不得仅凭 bbox 包含删除正文，不新增OCR。
可编辑单元格恢复另列未解决能力，不在消除重复后冒称表格已可编辑。

本轮未实施该修复。独立新页资格尚未确认、四页有内容/关系阻断，故不授予Beta候选资格。
用户接受、广泛发布及Windows支持仍未授予；旧U1裁决和历史验收状态保持。本轮到F3-03收口。
