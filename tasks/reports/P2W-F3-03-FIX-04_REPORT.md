# FIX-04：B表1与N4重复输出关闭

2026-09-22；承接已达到退出条件的FIX-03，串行实施。HEAD仍为 `e1eef44f8cd45b08e081ade3603cc31cbf9db25e`，未提交、未推送、未切默认。

## 修改

- 原生observation补充白色描边、线宽及端帽证据。合并表区域从实际轴向描边建立基础网格，按原绘制顺序计算完整边界区间的存在、缺失或不确定；不采样中点猜边，不用页号/哈希/表题特判。
- 仅跨完整缺失的边界合并基础格；外框必须闭合，合并结果必须为矩形且没有内部残留墙。端点只使用实际描边范围及float32数值精度解释。部分边界、L形区域、未知绘制、未匹配/跨格字形拒绝。白色遮盖若碰到已绘制的文字字形，也拒绝。
- 每个run由保留的实际字形范围唯一归入最终单元格，原文、原框及候选不改写。已知填充必须在所属格内且不遮盖已绘制字形。B的3条白色描边参与最终可见边界计算，并完整保留在原表图内。
- N4标题白色背景先检查所属对象、绘制顺序及后续字形，再在独立内存PDF副本中省略候选。2倍、4倍整页像素哈希均一致后，只取消重复背景裁剪；原PDF及源图不改。像素变化、渲染超界或验证异常均保留原策略并记录原因。移除对象先于父页面释放，重复执行资源测试未出现生命周期告警。
- 表格、图域候选发生路径/run/bbox归属冲突时撤销相关候选，不依赖调用顺序抢占。N1/N3严格规则、旧冻结作业和原生正文路径保留。

主要接缝为 `native_merged.py`、`native_background.py`、现有原生observation与提取入口。无新公共API、CLI参数、顶层IR schema、模型、服务或数据库。

## 验证

| 项目 | 实际结果 |
|---|---|
| B表1 | 11×5基础网格形成39个矩形单元格；51个run，0个无文字格；3条白色边界遮盖有证据 |
| B图1回归 | 4框、6条有向连接、20个run及可编辑图注保持 |
| B全作业 | 第4–5页共144个原生run（58+86），全部各归属一次；输出54个来源范围、2张图片 |
| N4表A.1 | 29×3基础网格形成62个矩形单元格；55个run、13个无文字格 |
| N4标题 | 3个背景矩形在2倍/4倍整页省略比较均无像素变化；5个标题run保持可编辑，只出现一次 |
| N4全页 | 70个run各归属一次，15个表外run不被吸收；输出13个来源范围、1张图片 |
| 图片 | 两张表图分别与旧完整表图的媒体SHA-256一致，没有重画或改正文 |
| 其他文字 | B、N4的表图外正文精确等于旧输出相应原生文字，源文拼写及空格不作常识纠正 |
| A/N1/N2/N3 | 正文XML、媒体和source-map块记录与原先相应核验输出完全一致；不把回归比较算作新增独立质量样本 |

B的一个 `w:tbl` 是图与图注的布局容器，不是数据表。两张数据表均为图片，**Word单元格可编辑性UNSUPPORTED**。内部单元格证据用于证明图域归属，不冒充可编辑表格交付。

### 实际 Word

macOS 26.6.2，Word 16.113.1（16.113.26091740），Python 3.12.13；依赖版本保存在 `final/environment.json`。

B连续两页作业：实际输出3页（FIX-03为4页）。全部3页检查，表1的39个格位、图1的全部框/连接/文字及图注完整。物理第4页仍重排为2个Word页，页眉/页码与原页不同。

N4：实际输出1页（原3页）。检查62个格位、13个空格、表题、标题和两段说明；整表及标题各出现一次，未发现局部遮挡或裁切。标题/正文/表题的字体、间距和对齐仍有差异。

两次Word打印PDF均有页边距提示，保留实际PDF核验。页数减少不是布局通过线；**布局仍LIMITED**。

分别在编辑副本插入 `B04EDIT`、`N4EDIT`，保存、关闭、重开并保留截图。独立OOXML核验确认仅增加相应标记，媒体及全部实际来源范围保持，自动原件哈希不变。B初次AX文本查询未显示标记，但重开截图及文件均确认标记存在；N4初次定位文本失败未写入，改用原页实际存在的正文后完成。没有借用其他文档的编辑记录。

## 工程检查与资源

- 第一轮全量1751 passed；随后增加“可选背景验证异常退出”和“白色遮盖字形拒绝”守卫与回归。背景异常用例先失败再修复。
- **最终 `uv run --locked pytest -q`：1753 passed，1条既有Starlette弃用警告，100.10秒，退出0。**
- **`uv run mypy .`：223个源文件，无问题，退出0。**
- `uv run --locked ruff check .`、`uv sync --locked --offline`、`git diff --check`成功。
- FIX-03的1次生成、FIX-04中间/最终各6次生成全部退出0；A仅冻结回放，其余从原PDF解析。每次实际命令、源哈希、退出码、墙钟及RSS见对应 `cases.json`，未以输出效果挑选样本。
- FIX-03-B、最终B、最终N4共3次离线再导出全部退出0，模型调用0，全部 `word/` 内容部件逐字节不变。
- 生成/再导出作业模型计数均0；socket拒绝钩子没有记录连接尝试。该监测不覆盖Word/macOS后台网络，不声称整个桌面零网络。未发起模型、上传、权重安装或FRP修改。

背景失败守卫不改变这两份成功输出。最终生成版本的全部Word XML、样式、关系及媒体与实际打开/编辑/打印版本逐字节一致，因此复用其Word证据；`final/word-equivalence.json`记录此核对，中间作业未覆盖。

## 证据

私有根：`tmp/docx-demo/f3-03-fix-04-20260922/`，**最终交付在 `final/`**。

- `baseline.json`、`baseline.diff`、`preservation.json`：FIX-03封存后的基线。
- `final/B/before-fix03.docx`、`before.docx`、`auto.docx`：原F3、FIX-03及FIX-04输出。
- `final/N4/before.docx`、`auto.docx`：同源前后输出。
- 两例的 `word-render.pdf`、全部 `word-page-*.png`、`body-edit.docx`及`edit-reopened.png`：实际Word与编辑证据。
- `final/jobs/`：完整IR、RenderPlan、observation、字形/格位/路径归属、source-map及源裁剪。
- `final/verification.json`、`audit.py`、`visual-checklist.json`、`reexport.json`：内容、来源、回退、视觉及再导出核验。
- `pytest-final.log`、`final/checks.json`及工具日志：最终检查；旧失败与中间检查日志保留。
- `final/preservation-final.json`：全部历史载荷哈希保持，FIX-03证据封存未变，本轮没有`.DS_Store`漂移。
- `final/final-source-state.json`与`final.diff`：最终源码状态；整个两片实施仅改动3个既有实现文件，其他已有未提交工作保持；新模块、测试、报告另列。

## 风险与退出

B分类图关系、B表1重复、N4表格及标题重复均有直接关闭证据。图内文字与数据表仍不可编辑，字号/间距/分页差异仍存在；复杂曲线、非矩形或不完整边界、未知遮盖及证据不足情况保持旧策略和明确拒绝原因，不扩称通用准确率。

FIX-03和FIX-04均达到本轮局部退出条件。用户接受PENDING，未执行F4-01。进入判断单列于 `P2W-F4-01_ENTRY_REVIEW.md`，不是Beta发布批准。
