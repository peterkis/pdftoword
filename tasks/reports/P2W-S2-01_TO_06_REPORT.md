# P2W-S2-01～S2-06 实施交付

2026-09-17；代码已完成用户人工核对，状态为 **ACCEPTED_WITH_FINDINGS**：内容、来源、可编辑性和图像结果可接受；排版与 PDF 原图仍存在差异，作为后续结构/排版主线发现保留。真实混合文档阶段验收尚缺相应输入。

## 可用能力

- pdf-inspector 1.20.0 已锁定，负责分类和原生文字；PDFium保留几何、渲染和对象边界。原extract签名、native/raster与Replay路径继续可用。
- 只读预检、连续四角变换、CropBox/页面与文字旋转、Form祖先矩阵、未知可见性、纯数据Observation已实现。
- auto将原生正文、配图与待处理区域分开；路由准备零请求，含糊区域经批准先PP判断，Ovis负责内容，PP正文不混入输出。
- CLI/API共用服务；区域计划绑定输入/裁剪/准备IR/目标/参数/预算，过期和变化被拒绝。一次执行一个新作业；取消停止后续发送，重复执行拒绝，auto/reviewed不覆写。
- 区域重建隔离并回映射；精确同位置同文字去重，原生候选和替代理由保留。原生/模型冲突继续审校，未确认的内容不静默删除。已有reviewed文稿时原自动计划发送前被拒绝，避免忽略人工选择。
- 单个有唯一PP几何对应的HTML表格只局部保图，周围正文可编辑；不宣称可编辑普通表格完成。合法图文组合可进入重建。

使用：[自动入口说明](../../docs/28_AUTO_MIXED_PDF.md)。依据：[后端与profile决策](../../docs/decisions/auto-mixed-native-backend-v1.md)。

## 本轮真实实物

用户指定两份PDF的物理页40–41、15–16。四页均只有整页图像，无原生文字对象，因此此次是真实扫描验证，不是原生＋扫描混合验收。

- 第一份两页：884个可编辑字符；DOCX结构有效；两页源内容自然重分页；Pages已打开查看。
- 第二份两页：1429个可编辑字符；一幅普通插图整体保留；普通表格局部源图降级；DOCX结构有效；Pages已打开、插入/删除中文测试行并另存检查副本。
- 两份原PDF及对应auto.docx的哈希核对未变；Pages检查操作仅作用于复制出的文稿。
- 真实网络请求共8次（PP 4、Ovis 4），均成功返回。初次第2份解析遇两处降级，依据保存响应新增回归并修复；复用3个成功响应，仅补发尚未执行的一次Ovis。旧作业完整保留。
- 来源准确率尚未逐字人工确认；保留文字几何和布局审校项，不能把生成成功写成内容质量通过。

私有文件位置（不纳入Git）：

- `tmp/docx-demo/s2-real-20260917-093943/demo-d34f05a7339646dbbf5fc8ac601e78aa/auto.docx`
- `tmp/docx-demo/s2-real-20260917-093943/demo-24bc01511929443b906cb63569538168/auto.docx`
- 第二份Pages检查副本在前一已检查作业 `demo-5dd0405c3661497aa8c58ba0dbe56d6d/pages-edit-check.pages`；第一轮、修复轮、授权与请求账本在各自独立目录。

最终交付重新复用8个成功响应、0新请求，以更新来源与候选记录。逐项比较两份新旧DOCX的全部 `word/` 包成员均字节一致，因此Pages检查覆盖相同的正文、样式与图片内容；前一检查文件没有覆写。

另外从冻结集development分区复用两份已授权N类电子文档，输出788/902可编辑字符，均0模型请求；第一份仍有区域待审校。自有中英电子夹具在Pages完成插入两行、删除和另存。受控混合夹具验证了原生正文保留、局部扫描文字可编辑、配图和局部失败隔离；没有冒充真实混合输入。

## 验证

- 最终完整pytest：**1278 passed**，0 fail/error/skip/XFAIL/XPASS。
- Ruff、旧97项任务目录、模型基线、Schema样例：PASS。
- 本次代码及对应测试的定向mypy：PASS。
- 完整quality：FAIL；仍为两份既有私有构建脚本共19个类型错误。没有修改它们或新增排除项。
- 外部计划包校验PASS；推荐器等11项工具测试PASS。
- `git diff --check`：PASS。
- 本机网页实际加载了真实作业、区域定位和结果；API上传回归实际生成DOCX且无网络请求。

本次新回归覆盖实际后端/CropBox/四方向/文字重定向、损坏与密码、空白标题、白色/隐藏文字、互斥恢复、真实PDF裁剪与DOCX、审批变更、取消、重复执行、局部HTTP失败、PP内容隔离、表格局部降级、离线成功响应复用、API与CLI共享路径。

## 用户人工验收

2026-09-17，用户完成对本轮 DOCX 的人工核对，明确反馈：除排版与 PDF 原图不一致外，其他结果可以接受。该反馈覆盖本次已提供输出中的正文内容、来源保留、可编辑性、图片/表格局部降级和无重复漏失观察；没有将排版差异记为通过，也没有将本次扫描页结果扩展为混合 PDF 的全面验收。

人工验收结论：`ACCEPTED_WITH_FINDINGS`。开放发现：Word/Pages 中的流式排版、字号/间距、图片位置和原 PDF 页面几何仍需后续排版主线处理；自动结果和人工版本继续分开保存。

机器证据：[CHECKS](P2W-S2-01_TO_06_CHECKS.json)。完整私有门禁目录为 `tmp/quality/s2-20260917-review-guard/`。

## 计划与未完成边界

外部计划依赖、目录JSON/CSV、阶段、提示和推荐器已同步；S1-04～06后置，S1已有状态/报告不改写。S2-07进入下一条结构主线。97个旧Ticket不自动关闭，原始计划ZIP/MANIFEST不重写。

真实混合PDF至少两份四页仍缺；用户提供的扫描书页不能抵充。这是阶段验收限制，不阻塞已实现能力的代码审阅。复杂多栏、图文冲突、多个无法唯一定位的表格保持保守审校；Word平台验收依用户决定后置。1～3页和25MiB限制保留。

本轮未commit/push/merge；仓库原有S1修改保留。公开记录不包含原文、图像、模型正文、密钥或用户绝对路径。

## 后续页码更正

用户将第1册改为物理第46–47页，旧结果仅作历史记录。最新结果与解析回归见[页码更正补充](P2W-S2_PAGE_CORRECTION_REPORT.md)。第2册不变。
