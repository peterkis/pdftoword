# P2W-F3-03-FIX-02：N3 多表重复输出修复

日期：2026-09-22。按用户批准计划实施：先补逐格字形证据，再消除局部重复输出。

## 修改

基线 HEAD 为 `e1eef44f8cd45b08e081ade3603cc31cbf9db25e`，分支 `codex/r2-03-columns-order`。保留先前 F1-04/F2-03/F3-03-FIX-01 未提交工作；本轮没有提交、推送、切换默认或进入 F4-01。

- `native_glyphs.py`：在 PDFium 锁内读取实际字形框、Unicode、字符索引、生成字符标记、文本对象及绘制顺序。与 pdf-inspector 原生 run 做精确非空白序列及位置匹配；不替换正文、不修正拼写、不改字体框。记录原文空白位置及匹配时跳过的字符索引。歧义、字符复用、异常字形、未知可见性和缺失几何拒绝归属。
- `input_analysis.py`：observation 附加字形映射及绘制顺序；仅由真实路径确认闭合轴对齐、无描边、不透明实心矩形。没有从包围框猜测矩形或网格。
- `native_grid.py`：保留原N1严格路径；新路径使用实际字形确认唯一单元格，完整线网格允许有证据的无文字格。底色必须落在一个格内；有文字格的底色须先于该格文本绘制。无文字格没有可比较的所属文本，因此要求没有未归属字形，并有同一行、相同颜色且已确认在文字之前绘制的底色作为依据。不会把前景遮盖当作底色，也不会直接丢弃所有填充路径。缺证据按表保留旧输出，拒绝原因进入 provenance。
- `native_pdf.py`：仅吸收已确认的线、底色路径和文字 run，每表输出一个 `table/preserve_image` 对象。候选中保留原文、字体、原框、run→字形→格位及路径映射；source-map 明确 `fallback=true`。
- 新增 `tests/demo/test_native_glyphs.py`，17个测试，含两种输出profile的三表DOCX集成、空格和生成字符、映射歧义/复用、跨格字形、未知绘制、断线/缺线及前景填充拒绝。

没有新增CLI参数、公共API、顶层Layout IR schema、OCR或服务。旧作业缺少字形证据时使用原路径，回放不重新读取原PDF补证据。

## 验证

固定源：WS/T 305—2023物理第24页，源文件SHA-256：`33792a20fb688fbe762ac0a2e969f61f48ef2c55b93905e9925e5c37a274b907`。转换直接读取原PDF，batch4标注未进入转换输入。

| 区域 | 格位 | 保留原生run | 无文字格 | 底色路径 |
|---|---:|---:|---:|---:|
| 第一表 | 9×5＝45 | 47 | 3 | 5 |
| 第二表 | 20×5＝100 | 102 | 2 | 5 |
| 第三表 | 10×5＝50 | 53 | 2 | 5 |
| 合计 | 195 | 202 | 7 | 15 |

全页215个run均保留且各归属一次；202个表内run有唯一逐格字形证据，13个表外run未被吸收。字符索引没有重复占用。表外最终OOXML文字与旧输出中相应文字精确相同。

N3从91个块、39张图片变为11个块、3张图片。三张新表图分别与旧完整表图的媒体SHA-256相同；没有重画或静默纠正源文拼写。三个标题及表外正文保留；仍有0个Word数据表，**可编辑表格为UNSUPPORTED**。

N1、N2、N4的 `word/document.xml`、媒体及source-map块记录与FIX-01最终作业完全相同。N4没有因允许空格而误获网格支持。这是输出回归控制，不新增其Word视觉验收或关闭其旧缺陷。

### 实际 Microsoft Word

平台macOS 26.6.2，Word 16.113.1（16.113.26091740）。打开独立 `word-view.docx`，通过Word本地打印PDF导出；保留“小页边距可能裁切”提示的核验说明，没有调用LibreOffice或网页渲染代替Word。

- 实际输出由原4页变为1页，检查了全部输出页。
- 对照原页检查三表全部195个格位、7个无文字格、15处底色、边框、三个标题及顺序；三张表各出现一次，没有额外文字行或线段。表图中的源文拼写保留，未发现局部遮挡或裁切。
- 字体、正文段距、页眉/页码位置及表间间距仍与原页不同，**布局为LIMITED**；1页不是通过线。
- 在独立编辑副本对表外正文插字、保存、关闭、重开。输入动作使用带方括号的标记，实际保存为 `N3EDIT`；以实际文件及重开后的Word状态为准。原文仅增加这6个字符，三张图片哈希和11个来源书签保持；进一步核对每个来源范围仍包围其实际正文/图片，三表图片均留在原来源范围内。
- 自动原件哈希不变。此编辑是诊断操作，不计识别成绩；没有构造可编辑表格充数。

### 命令与工程检查

| 命令/操作 | 结果 |
|---|---|
| 新多表回归实施前 | 1 failed / 3 passed，缺少glyph接口；记录在 `red.log` |
| `uv sync --locked --offline` | 退出0 |
| `uv run --locked pytest -q` | 退出0，1725 passed，1条既有Starlette弃用警告，100.77s |
| `uv run --locked ruff check .` | 退出0 |
| mypy检查4个修改源文件 | 退出0 |
| `uv run --locked mypy .` | 退出2：历史临时目录同名 `writer-before.py` 模块冲突导致检查中断；不宣称全仓类型检查通过 |
| 现有CLI native转换N1/N2/N3/N4 | 4个作业全部退出0；单作业约0.66–1.20s，真实命令、RSS及源哈希见 `cases.json` |
| 现有CLI `replay-job` 再导出N3 | 退出0，模型调用0，正文XML/媒体与自动原件一致 |

4次转换及1次离线再导出均受socket连接拒绝钩子监测，连接尝试记录为空，作业模型计数均0。该钩子只覆盖受监测的Python进程，不把Word/macOS后台网络算作已证明为0；本轮没有外发操作。

独立既有DOCX reader仍返回 `UNSUPPORTED_TEXT_POSITION`，没有将其空结果作为检查成功；补以独立原始OOXML、媒体、来源范围核验和实际Word证据。

## 证据

私有根目录：`tmp/docx-demo/f3-03-grid-fix-02-20260922/`。原始正文、图像和产物不进入Git。

- `baseline.json`、`baseline.diff`、`preservation.json`：HEAD、已有改动及历史文件集合/哈希。首次快照辅助脚本不存在，先创建了一个新回归测试；基线明确排除此本轮新文件，所有既有文件在快照时尚未修改。
- `N3/before.docx` / `N3/auto.docx`：同源前后输出；`N3/before-word-render.pdf`为旧F3-03已有Word证据副本，未冒称本轮重跑。
- `N3/word-render.pdf`、`N3/word-page-1.png`、`N3/word-screen.png`：本轮实际Word输出；`N3/body-edit.docx`、`N3/edit-reopened.png`：保存重开证据。
- `jobs/`：新IR、RenderPlan、source-map、observation及源裁剪；`observation-probe.json`保留初始诊断。
- `verification.json`、`edit-verification.json`：逐格数、run/字形守恒、媒体、11个来源范围及编辑守恒；`reexport.json`：离线再导出结果。
- `checks.json`及对应日志：实际命令和退出码；`environment.json`：Python 3.12.13、pdf-inspector 1.20.0、PDFium包装库5.13.0等实际依赖版本。
- `preservation-final.json`：源PDF、标注及各历史证据的文件集合和载荷哈希保持，当前核验没有 `.DS_Store` 漂移；先前未提交文件除本轮明确修改的3个实现文件外均保持。

## 风险与退出结论

N3局部重复内容缺陷已有工程、来源守恒及实际Word证据。它已经是调试样本，不能计作新的独立验证页；没有推导通用准确率或性能分位数。

合并格、未知绘制、缺字/歧义、不能证明所属对象/可见性、字形真正跨格、图像遮盖及不完整网格仍退出新路径。没有用更大的数值容差强行接受。N1旧严格路径保持；这不将其历史证据升级为本轮逐字形验收。

**表格可编辑性UNSUPPORTED，布局LIMITED，用户接受PENDING。** B分类图关系和N4复杂表格等原阻断仍在，不进入F4-01。

唯一下一建议：处理B分类图的框、箭头及文字被拆散导致的关系丢失；先确认完整图域与所属文字证据，不把本轮表格规则直接套用到分类图。本轮不自动执行。
