# R1-04 真实样本补证与收口建议

## 结论

现有可核验样本未产生符合当前门禁的采用结果。**真实采用与真实布局收益均保持 NOT_VERIFIED**，没有为了做出对照调整阈值、模糊匹配原文或指定 Monkey 胜出。

建议按已知限制冻结工程实现与这些失败/弃权案例，单独保留真实采用/收益验收缺口；不把它写成完整 R1-04 验收通过，也不为制造赢家继续扩展本轮范围。R1-06 的既定工程前置为 R1-01/R1-05，可另行推进，不需要重做已合并的 R1-05。

本次仅新增审计产物和报告，没有修改生产代码或策略。未提交、未推送，真实模型请求 0。

## 第 47 页补证

固定使用 R1-03 本次真实作业的 Ovis 内容，不替换为更容易匹配的历史正文。

| 项目 | 结果 |
| --- | --- |
| 当前独立绑定 | 7/16，仍 ABSTAIN |
| 另外 2 块 | Ovis/PP 的句末标点不完全一致 |
| 另外 7 块 | 填空下划线缺失；其中 1 块还包含跨行连字符差异 |
| 旧同页 PP 证据 | 两个历史作业的页面 prunedResult 与当前一致，不能增加独立定位证据 |
| 新规则或正文处理 | 没有加入宽松标点归一化、删除下划线、去连字符或 fuzzy threshold；没有修改正文 |

只在诊断中寻找最接近的行并记录差异区间/字符类别，不把诊断相似度送入仲裁器。无法补足严格匹配的 9 块保持未绑定，不按长度分框，也不将“接近”升级为已证实。

记录更正：先前中间表述将旧 PP 与当前响应误称为完整字节相同；实际只有页面解析结果相同，顶层 logId 不同。旧/新完整响应 hash、初版 receipt 和更正 receipt 均保留，绑定结论未变。

## 检查范围与固定比较方式

范围来自现有 S2 交付报告、页码更正报告和 T0016 封存目录：**5 份真实来源、9 个不同物理页**。其中 7 页有历史模型证据，另外 2 页为已有真实原生电子样本。

检查各个旧作业版本而不只选取结果最好的版本；它们是重复版本对照，不能充作独立样本。共检查 18 条历史页面版本记录，以及数学试卷 JPG/PNG 各 3 组几何响应，共 6 组比较。

数学试卷固定每种格式唯一已有的 Ovis repeat 1 内容，逐一比较同一输入的 PP/Monkey repeat 1/2/3；每组独立保留响应来源、hash 和决策。不跨组拼框、不挑选最佳结果，不伪装成新的模型请求，也不改写 T0016 原评测。

| 实际来源范围 | 支撑情况 | 当前结果 |
| --- | --- | --- |
| 第 1 册物理页 40 | 交付版本 9/14 块 | ABSTAIN |
| 第 1 册物理页 41 | 交付版本 3/6 块 | ABSTAIN，含对应关系歧义 |
| 第 2 册物理页 15 | 交付版本 5/18 块 | ABSTAIN |
| 第 2 册物理页 16 | 交付版本 3/4 块 | ABSTAIN，含对应关系歧义 |
| 第 1 册物理页 46–47 | 各历史版本保留；本次为 0/1 与 7/16 | 均 ABSTAIN |
| 数学试卷，JPG/PNG 六组 | 每组固定内容 45 块，26 块有支撑 | 全部 ABSTAIN；宏框重叠或对应关系不唯一/缺失 |
| 两份真实原生样本，各 1 页 | 原生支撑分别 63/63、23/23 | 无 PP/Monkey 宏响应；已有几何也未证明新的列顺序，保持原结果 |

18 条历史记录中有 4 条早期版本缺失 stored_response_sha256 和 raw_response_path；没有补写“历史封存”，这些条目标 NOT_VERIFIED。两条原生记录的宏结构采用也为 NOT_VERIFIED。其余 12 条历史记录和 6 组数学对照均实际调用当前支持构造与仲裁器，结果为 ABSTAIN。不能把未验证记录算成模型质量失败。

T0016 更早的 PARTIAL run 仍由原 `REPLAY_REQUIRES_COMPLETE_RUN` 边界排除；其全部封存文件核对不变，未重算评测或晋级状态。它属于同一数学试卷，不是新增物理样本。受控混合夹具、单元测试和副本不计入真实样本数。

## 证据与验证

私有根：`tmp/docx-demo/r1-04/sample-audit/`。

- `page47-diagnosis.json`：9 块差异区间与字符类别，仅诊断。
- `historical-pages.json` / `historical-pages-clarified.json`：全部历史页面版本、hash 核验、逐块账本与仲裁理由；保留原记录，再明确旧版本缺失的历史 hash 字段。
- `math-{jpg,png}-{1,2,3}.json`、`math-summary.json`：6 组独立比较及原始请求来源；`math-baselines/` 保存两份新生成的固定内容基线 DOCX/IR，模型请求 0。
- `native-order-audit.json`：两份原生样本独立支撑与列顺序弃权依据。
- `response-comparison-correction.json`：PP 完整字节比较的更正与实际 hash；`audit-receipt.initial.json` 不删除。
- `audit-receipt.json`：范围、计数、策略指纹、源文件不变核验与产物 hash。

实际使用现有 `replay.load`、`content_support`、`GeometryArbitrator.propose`、`column_order` 执行离线检查，退出码 0。数学基线实际经过 source_image/reconstruct/finish，无模型请求。未读取 ground truth 作为生成或绑定输入。

不变性检查均成立：18 份源作业快照不变，完整 T0016 run 的 35 个封存文件及早期 PARTIAL run 的 34 个封存文件不变。历史输入图及响应只有与现有请求记录 hash 匹配时才作为可用绑定证据；saved IR 的本次 hash 仅记为当前快照，不冒充历史封存。

生产源码与上次 1531 项测试对应 hash 一致，策略指纹仍为 `6264f01eac963c5b3f026a36a69fed45e9adf8dfa693ab4d0fc476696459f532`。本次不因只读检查重复宣称跑过全套测试；最终执行 `git diff --check`。

## 已知限制

- 本结论只覆盖上述已登记、可定位的现有样本和当前保守策略，不外推成 Monkey/PP 普遍无效。
- R1-03 内容回退、字体显示和布局含糊继续独立保留；未修正文、未修改字体。
- 工程测试和正确弃权证明了边界，不等于真实采用/改善验证。
- 复杂绑定仍可作为后续明确任务，但不能以本次缺少正例为由放宽门槛。
