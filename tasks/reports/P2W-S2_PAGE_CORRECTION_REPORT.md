# 第1册页码更正补充记录

用户将第1册PDF物理页由40–41更正为46–47；书内印刷页码为40–41。第2册物理页15–16不变。此前的作业、报告和响应保留，不改写历史。

本次沿用用户明确批准的PP/Ovis服务与不限请求次数授权。新页共4次HTTP请求（PP/Ovis各2次），全部返回成功，原PDF哈希未变。

第47页的填空被识别为独立的 `___`，旧解析器把它当作不支持的Markdown，触发区域降级。新增真实DOCX回归后，修正语法判定：仅在检测中屏蔽行内、空白分隔的连续下划线，输出仍保留原始字符；独占一行的分隔语法及强调标记继续按原策略审校。

复用4个已保存响应、0新请求重建：783个可编辑字符、两幅配图，DOCX结构有效。该结果的COMPLETE是执行完成，不是逐字准确率验收；两页仍属整页扫描。

新输出：`tmp/docx-demo/s2-page-correction-20260917-114211/demo-59fe1fa94735418fb118653b06fa961d/auto.docx`。

验证：相关原型回归245项通过；最终调整后的Ovis/区域回归通过，Ruff、定向mypy与diff检查通过。未重新运行完整门禁，先前1278项记录继续代表前一代码状态。工具侧 Pages 文件选择器曾因打开按钮不可用而未形成独立工具验收记录；随后用户已完成人工核对，反馈除排版与 PDF 原图不一致外其他可以，因此本补充记录的人工结论为 `ACCEPTED_WITH_FINDINGS`，不把排版差异记为通过。
